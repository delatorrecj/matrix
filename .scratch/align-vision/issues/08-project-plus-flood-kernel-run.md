# 08 — Project + flood as one kernel run

**What to build:** A planner can ask what happens if the intervention coincides with a flood closure and get ECO-4 / resilience scores from that same trajectory. Flood is not only a map overlay. VAL-02 may remain NOT_RUN until sourced 2024 closure geometry exists; this ticket must not publish a placeholder IoU as a pass.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] A flood + project (or flood + closure) scenario sets flood-related trajectory meta so ECO-4 is not stuck at honest-zero for lack of flags.
- [ ] All five modules still score that one trajectory; synthesis cites ECO-4 only when the kernel emitted it.
- [ ] VAL-02 stays NOT_RUN (or FAIL) unless a non-provisional fixture is present; placeholder polygons are not shown as validation success.
- [ ] A reviewer can demo: Diversion (or equivalent) closed by flooding → playback + ecological flood-exposure metric moves for the right reason.
