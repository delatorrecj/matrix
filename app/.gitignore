# --- Secrets (never commit; CLR-matrix RA 10173) ---
.env
.env.local
*.key
credentials.json
token.json

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# --- Node / Next.js ---
node_modules/
.next/
out/
.turbo/
npm-debug.log*

# --- SUMO / simulation outputs (regenerable) ---
*.sumocfg.out
tripinfo*.xml
fcd-output*.xml
*.tmp

# --- Local data / caches (regenerable; raw data lives in ../data, gitignored there) ---
.chroma/
*.duckdb
.tmp/

# Generated validation report (CR-007 PR 5b): a computed, baseline-dependent artifact
# produced by `python -m matrix_kernel.build_validation_report`. GET /validation serves it
# when present, else the live module reports NOT_RUN. Deliberately NOT committed — generate
# it at deploy once demand is calibrated; an uncalibrated VAL-01 is withheld, not shipped.
validation_report.json
packages/kernel/validation_report.json
