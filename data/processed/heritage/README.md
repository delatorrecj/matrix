# NHCP / OSM heritage interim

SOCI-2 currently uses OSM `historic=*` tags (INVENTORY OSM-ILO).

When an NHCP declared-site list is obtained, save:

```
nhcp_iloilo_sites.csv
```

Suggested columns: `name,barangay,lat,lon,declaration_year,source`

Do not invent coordinates or declarations.
